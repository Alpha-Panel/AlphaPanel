# Mailu env overrides (optional). Most env vars come from the parent docker-compose
# via the `x-mailu-environment` anchor in mailu.yaml. Add per-deployment overrides
# here only if you need to deviate from the root `.env` defaults.
#
# Examples:
# AUTH_RATELIMIT_USER=50/day
# MESSAGE_RATELIMIT=100/hour
